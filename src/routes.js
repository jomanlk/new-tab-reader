import Landing from './components/Landing';
import ViewFeeds from './components/ViewFeeds';

const routes = [
  { path: '/', component: Landing },
  { path: '/feeds', component: ViewFeeds },
];

export default routes;
